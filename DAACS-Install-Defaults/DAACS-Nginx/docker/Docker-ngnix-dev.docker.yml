services:
  #nginx_service_name:
    build: 
      context: .
      dockerfile: ./Dockerfile-ngnix
    restart: unless-stopped
    container_name: nginx-proxy-cache-dev
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock
      - /var/www/html/certs:/etc/nginx/certs
      - ./nginx/vhost.d:/etc/nginx/vhost.d:ro 
    network_mode: host
