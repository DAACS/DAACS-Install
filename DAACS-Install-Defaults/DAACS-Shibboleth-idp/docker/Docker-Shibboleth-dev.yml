services:
  shibboleth_real:
    expose:
      - 80
      - 443
    restart: unless-stopped
    networks:
      - myNetwork
    env_file: 
      - ${ENV_DIR}shibboleth
      - ${ENV_DIR}ldap
    # volumes:
    #   - "${FOLDER_START}:/usr/src/app"
    image: shibbolethreal

networks:
  myNetwork:
    external: true
